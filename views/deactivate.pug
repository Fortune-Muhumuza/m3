extends layout

block content
    div(class="columns")
        div(class="column")
            h2(class="subtitle") are you sure you want to delete your account?

            a(class="button is-link", href="/") Bring me back
            
            form(action="/deactivate", method="POST")
                input(class="button is-danger", type="submit", value="Deactivate Account")