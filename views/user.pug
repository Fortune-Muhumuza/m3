extends layout

block content
    section(class="hero")
        div(class="hero-body")
            div(class="container")
                h1(class="title") #{user.username}!
                h2(class="subtitle") #{user.bio}